#include "faisceaux.h"
#include "accelerateur.h"
#include "elements.h"
#include  <math.h>
#include "vecteur3D.h"

/*Faisceaux::Faisceaux(int nombre_particules, int lambda, double charge, double mass,Vecteur3D v,SupportADessin* support,Accelerateur* acc): Dessinable (support):
lambda(lambda),nombre_particules(nombre_particules)
{
    int N = nombre_particules/lambda;
    for (int i=0;i<N;i++) {
        double alpha = (2*pi)
    }
}
*/
